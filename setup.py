from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A simple machine learning model to detect spam from several entries of emails. The dataset its from Kaggel',
    author='Pedro Ortiz',
    license='MIT',
)
